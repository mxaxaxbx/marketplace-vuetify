import { GetterTree } from "vuex";
import { ProductsI } from "../interfaces/products.interface";
import { RootStateI } from "../interfaces/types.interface";

export const getters: GetterTree<ProductsI, RootStateI> = {
}
